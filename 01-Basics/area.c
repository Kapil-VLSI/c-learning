#include<stdio.h>
int main()
{
    int side;
   printf("enter the side of square "); 
   scanf("%d",&side);
 int area = side*side;
   printf( "area of a square is %d " , area);
   return 0;
}